interface Props {
  className: string;
}

const PositionInfo = ({className}: Props) => {
  return (
    <section className={`${className}`}>
      PositionInfo
    </section>
  )
}

export default PositionInfo